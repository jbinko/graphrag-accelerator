name: MSD
run-name: MSD
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: ./setup_build.sh
      - name: Check out repository code
        uses: actions/checkout@v4
